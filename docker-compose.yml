version: '3.8'
services:
  react-app:
    container_name: react-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8100:80"
    volumes:
      - .:/app
      - /app/node_modules
